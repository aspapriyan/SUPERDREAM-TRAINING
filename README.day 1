Array is storing multiple values in same datatype
Array // int[] arr = {10,20,30}
Array index start from 0;

Method 1 // int[] arr = {10,20,30,40};
Method 2 // int[] arr = new int[4];
                  arr[0] = 10;
                  arr[1] = 20;
                  arr[2] = 30;
                  arr[3] = 40;

Array traversal: Accessing each element one by one using for loop

 for(int i = 0; i < arr.length; i++) {
    System.out.println(arr[i]);
}

Array insertion: We must move elements to right and inserting elements should be done from right side that is from backwards until the insertion postion is reached

Array deletion: We must delete elements from right and first we have to move and then remove last

for(int i=pos;i<arr.length --1;i++){
     arr[i]=[i+1];

Searching: Finding a particular element in a collections of element

TYPES: LINEAR
       BINARY

Linear search: Searching an element one by one form the beginning till the element found

LOGIC:
 key=30;
for(int i=0;i<arr.length;i++)
if(arr[i] == key)
// print

Binary search: Binary search is faster than linear search and it must be strictly in sorted order

We use 3 variables:

low 
high
mid

low = 0;
high = arr.length -1;
mid = (low+high) / 2;
